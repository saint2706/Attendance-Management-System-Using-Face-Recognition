The instructions requested to ensure Dockerfiles are optimized for security, size, and speed. Upon thorough investigation using hadolint, inspecting the Dockerfiles line-by-line, verifying against memory rules (multi-stage builds, non-root users with --system, cleanup commands attached to the same RUN block, pinned base images, extensive .dockerignore), and successfully passing all tests and linting locally, it has been verified that the application's Docker configurations (Dockerfile and Dockerfile.gpu) are already fully optimized and aligned with the given boundaries.
